# ── API URL ─────────────────────────────────────────────────────────────────
# In development: leave empty — Vite's proxy will forward /api → localhost:5000
# In production (if deploying frontend separately): set to your backend URL
# Example: VITE_API_URL=https://smart-crop-advisor.onrender.com
VITE_API_URL=
